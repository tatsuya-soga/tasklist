<h1>作成ページ</h1>

<%= from_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>
